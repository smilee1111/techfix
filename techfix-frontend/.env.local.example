# Copy this file to .env.local and adjust if your backend runs elsewhere.
# Not required for local dev — defaults to http://localhost:5000/api if unset.

NEXT_PUBLIC_API_URL=http://localhost:5000/api
